# Required environment variables for this sandbox.
# Each ${VAR} referenced anywhere in config.yaml (including plugin options)
# must be set here. Values are never generated — fill them in locally.
AGW_LLM_GATEWAY_API_KEY=
FAZZ_VPN_CONFIG_B64=
FAZZ_VPN_TOTP_SECRET=
FAZZ_VPN_USERNAME=
GITHUB_PAT=
